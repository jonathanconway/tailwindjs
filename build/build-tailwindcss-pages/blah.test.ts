import { load } from "cheerio";

// import { ASPECT_RATIO_HTML } from "./tailwindcss-pages";

describe("abc", () => {
  it("blah", () => {
    // const $ = load(ASPECT_RATIO_HTML.html);
    // console.log($("h5"));
  });
});
